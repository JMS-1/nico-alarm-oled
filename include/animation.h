#pragma once

class Animation
{
public:
    static void startAnimation(const String &message);
    static void animate();
    static void writeText(const String &message);
    static void setup();
};
